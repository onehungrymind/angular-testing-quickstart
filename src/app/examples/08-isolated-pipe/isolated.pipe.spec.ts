import { IsolatedPipe } from './isolated.pipe';

describe('IsolatedPipe', () => {
  // How would you instantiate the pipe in isolation?

  it('adds an exclamation mark to input', () => {
    // What can you call on the pipe and what can we expect?
  });
});
